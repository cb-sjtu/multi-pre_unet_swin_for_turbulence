_target_: src.models.base.lstm_1plane.LSTM1Plane

# Model architecture for 1-plane 3-channel flow prediction
input_shape: [256, 256]  # Higher resolution than 3-plane
num_channels: 3  # 1 plane × 3 fields (u,v,w)
sequence_length: 5
prediction_horizon: 1

# LSTM architecture
hidden_dimensions: [16, 32, 32, 64, 64]
kernel_sizes: [2, 2, 2]
num_layers: 3
bias: true
